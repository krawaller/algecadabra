import { Language } from '../../types'
import { englishEntities } from './entities'
import { englishErrors } from './errors'
import { englishSpells } from './spells'

export const english: Language = {
  entities: englishEntities,
  errors: englishErrors,
  spells: englishSpells,
}
